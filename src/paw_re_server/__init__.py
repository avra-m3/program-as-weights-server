def main() -> None:
    print(
        "paw-re-server has no default task yet.\n"
        "Run the test harness directly instead, e.g.:\n"
        "  uv run python -m paw_re_server.runner --spec specs/<task>.txt "
        "--data data/<task>.yaml"
    )
