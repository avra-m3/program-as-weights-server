stages:
  - deploy

deploy:
  stage: deploy
  rules:
    - if: $CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE == "push"
  trigger:
    project: example-org/downstream-project
    branch: main
    strategy: depend
